# Monitor-Feature Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Automatisches Server-Monitoring mit PID/A2S-Check, Auto-Restart (5×/h), Pause bei manuellem Stop, und Player-Count-Anzeige in manage.cgi.

**Architecture:** Ein System-Cron-Job ruft alle 2 Minuten `monitor_all.sh` auf, das für jede registrierte Instanz `monitor_instance.sh` startet. LGSM-Server delegieren an LGSM's eigenes `monitor`-Kommando; Non-LGSM-Server (Windrose/Wine) durchlaufen PID-Check + A2S-UDP-Query + eigene Restart-Logik. State wird in `$config_dir/monitor/<id>/state` persistiert. Perl-seitig liest `manage.cgi` State und führt A2S-Query für den Player-Count durch.

**Tech Stack:** Bash (Shell-Watchdog), Pure Perl (A2S via IO::Socket::INET), TAP-Tests, Webmin ui_* API, JSON::PP

---

## Datei-Überblick

| Datei | Aktion | Zweck |
|-------|--------|-------|
| `src/lang/de` | Modify | Monitor-Strings auf Deutsch |
| `src/lang/en` | Modify | Monitor-Strings auf Englisch |
| `src/lib/games_meta.json` | Modify | `query_port_field` pro A2S-Game |
| `src/lib/games_meta.pl` | Modify | `get_game_query_port_field()` |
| `src/lib/monitor.pl` | Create | State lesen/schreiben |
| `src/lib/query.pl` | Create | Pure-Perl A2S-Query |
| `src/scripts/query_a2s.pl` | Create | CLI-Wrapper für A2S |
| `src/scripts/monitor_instance.sh` | Create | Per-Instanz-Monitor |
| `src/scripts/monitor_all.sh` | Create | Watchdog für alle Instanzen |
| `src/manage.cgi` | Modify | Player-Count + State-Writes + Reset-Button |
| `src/index.cgi` | Modify | Monitor-Status-Badge in Instanzliste |
| `t/test_monitor_state.pl` | Create | Tests für monitor.pl |
| `t/test_a2s_query.pl` | Create | Tests für query.pl |

---

## Task 1: Lang-Strings + games_meta.json + games_meta.pl

**Files:**
- Modify: `src/lang/de`
- Modify: `src/lang/en`
- Modify: `src/lib/games_meta.json`
- Modify: `src/lib/games_meta.pl`

- [ ] **Step 1: Lang-Strings anhängen**

`src/lang/de` am Ende:
```
monitor_status_running=Monitor: Aktiv
monitor_status_failed=Monitor: Fehlgeschlagen (5 Neustarts/h erreicht)
monitor_status_paused=Monitor: Pausiert (manuell gestoppt)
monitor_status_disabled=Monitor: Deaktiviert
monitor_reset_btn=Monitoring aktivieren
monitor_disable_btn=Monitoring deaktivieren
monitor_players=Spieler
monitor_col=Monitor
```

`src/lang/en` am Ende:
```
monitor_status_running=Monitor: Active
monitor_status_failed=Monitor: Failed (5 restarts/h reached)
monitor_status_paused=Monitor: Paused (manually stopped)
monitor_status_disabled=Monitor: Disabled
monitor_reset_btn=Enable monitoring
monitor_disable_btn=Disable monitoring
monitor_players=Players
monitor_col=Monitor
```

- [ ] **Step 2: query_port_field in games_meta.json hinzufügen**

Für jedes A2S-fähige Spiel ein `"query_port_field": "queryport"` auf der obersten Ebene des Game-Eintrags hinzufügen. Minecraft-Varianten bekommen KEINEN Eintrag (kein A2S).

Betroffene Einträge: `vhserver`, `pwserver`, `csgoserver`, `rustserver`, `armaserver`, `windrose`.

Beispiel für `vhserver` (nach `"steam_required": ...`):
```json
"query_port_field": "queryport",
```

Gleiches für `pwserver`, `csgoserver`, `rustserver`, `armaserver`, `windrose`.

- [ ] **Step 3: get_game_query_port_field in games_meta.pl**

In `src/lib/games_meta.pl` nach `sub get_game_source` einfügen:

```perl
sub get_game_query_port_field {
    my ($script) = @_;
    my $db = _load_meta();
    return '' unless defined $db->{$script};
    return $db->{$script}{'query_port_field'} // '';
}
```

- [ ] **Step 4: Syntax-Check**

```bash
perl -c src/lib/games_meta.pl
python3 -c "import json; json.load(open('src/lib/games_meta.json'))" && echo "JSON OK"
```
Erwartetes Ergebnis: beide Befehle ohne Fehler.

- [ ] **Step 5: Commit**

```bash
git add src/lang/de src/lang/en src/lib/games_meta.json src/lib/games_meta.pl
git commit -m "feat(monitor): lang-strings, query_port_field in games_meta, get_game_query_port_field"
```

---

## Task 2: src/lib/monitor.pl + t/test_monitor_state.pl

**Files:**
- Create: `src/lib/monitor.pl`
- Create: `t/test_monitor_state.pl`

- [ ] **Step 1: Test-Datei schreiben**

`t/test_monitor_state.pl`:
```perl
#!/usr/bin/perl
use strict;
use warnings;
use File::Temp qw(tempdir);
use FindBin qw($Bin);
use lib "$Bin/..";
chdir "$Bin/.." or die "Cannot chdir: $!";

print "1..8\n";
sub pass { print "ok - $_[0]\n" }
sub fail { print "not ok - $_[0]\n" }

my $tmp = tempdir(CLEANUP => 1);

require './src/lib/monitor.pl';

# 1. read_monitor_state returns defaults for missing file
{
    my $s = read_monitor_state($tmp, 'testinst');
    ($s->{status} eq 'running' && $s->{restart_count} == 0)
        ? pass('read_monitor_state: defaults for missing state file')
        : fail("read_monitor_state: wrong defaults: status=$s->{status} count=$s->{restart_count}");
}

# 2. write then read round-trip
{
    write_monitor_state($tmp, 'testinst', { status => 'failed', restart_count => 5, window_start => 1000 });
    my $s = read_monitor_state($tmp, 'testinst');
    ($s->{status} eq 'failed' && $s->{restart_count} == 5)
        ? pass('write/read monitor state round-trip')
        : fail("write/read round-trip failed: status=$s->{status} count=$s->{restart_count}");
}

# 3. set_monitor_paused writes paused
{
    set_monitor_paused($tmp, 'inst2');
    my $s = read_monitor_state($tmp, 'inst2');
    $s->{status} eq 'paused'
        ? pass('set_monitor_paused writes paused')
        : fail("set_monitor_paused: status=$s->{status}");
}

# 4. set_monitor_running resets count and writes running
{
    write_monitor_state($tmp, 'inst3', { status => 'failed', restart_count => 5, window_start => 1 });
    set_monitor_running($tmp, 'inst3');
    my $s = read_monitor_state($tmp, 'inst3');
    ($s->{status} eq 'running' && $s->{restart_count} == 0)
        ? pass('set_monitor_running resets count')
        : fail("set_monitor_running: status=$s->{status} count=$s->{restart_count}");
}

# 5. set_monitor_disabled writes disabled
{
    set_monitor_disabled($tmp, 'inst4');
    my $s = read_monitor_state($tmp, 'inst4');
    $s->{status} eq 'disabled'
        ? pass('set_monitor_disabled writes disabled')
        : fail("set_monitor_disabled: status=$s->{status}");
}

# 6. monitor_is_active: running -> true
{
    write_monitor_state($tmp, 'active', { status => 'running', restart_count => 0, window_start => time() });
    monitor_is_active($tmp, 'active')
        ? pass('monitor_is_active: running -> true')
        : fail('monitor_is_active: running should be active');
}

# 7. monitor_is_active: paused -> false
{
    write_monitor_state($tmp, 'paused', { status => 'paused', restart_count => 0, window_start => time() });
    !monitor_is_active($tmp, 'paused')
        ? pass('monitor_is_active: paused -> false')
        : fail('monitor_is_active: paused should not be active');
}

# 8. monitor_is_active: disabled -> false
{
    write_monitor_state($tmp, 'dis', { status => 'disabled', restart_count => 0, window_start => time() });
    !monitor_is_active($tmp, 'dis')
        ? pass('monitor_is_active: disabled -> false')
        : fail('monitor_is_active: disabled should not be active');
}
```

- [ ] **Step 2: Test ausführen (muss scheitern)**

```bash
perl t/test_monitor_state.pl 2>&1
```
Erwartet: Fehler wegen fehlendem `./src/lib/monitor.pl`.

- [ ] **Step 3: src/lib/monitor.pl implementieren**

```perl
# LinuxGSM-WebCore - Monitor state management
use strict;
use warnings;

return 1 if defined &read_monitor_state;

sub _state_file {
    my ($config_dir, $id) = @_;
    return "$config_dir/monitor/$id/state";
}

sub read_monitor_state {
    my ($config_dir, $id) = @_;
    my %defaults = (status => 'running', restart_count => 0, window_start => time());
    my $file = _state_file($config_dir, $id);
    return {%defaults} unless -f $file;
    my %state = %defaults;
    open(my $fh, '<', $file) or return {%defaults};
    while (<$fh>) {
        chomp;
        next unless /^(\w+)=(.*)$/;
        $state{$1} = $2;
    }
    close($fh);
    $state{restart_count} = int($state{restart_count} // 0);
    $state{window_start}  = int($state{window_start}  // 0);
    return \%state;
}

sub write_monitor_state {
    my ($config_dir, $id, $state_ref) = @_;
    my $file = _state_file($config_dir, $id);
    my $dir  = $file;
    $dir =~ s|/[^/]+$||;
    unless (-d $dir) {
        require File::Path;
        File::Path::make_path($dir) or return;
    }
    open(my $fh, '>', $file) or return;
    print $fh "status=$state_ref->{status}\n";
    print $fh "restart_count=" . int($state_ref->{restart_count} // 0) . "\n";
    print $fh "window_start=" . int($state_ref->{window_start} // time()) . "\n";
    close($fh);
}

sub set_monitor_paused {
    my ($config_dir, $id) = @_;
    my $s = read_monitor_state($config_dir, $id);
    $s->{status} = 'paused';
    write_monitor_state($config_dir, $id, $s);
}

sub set_monitor_running {
    my ($config_dir, $id) = @_;
    write_monitor_state($config_dir, $id, {
        status        => 'running',
        restart_count => 0,
        window_start  => time(),
    });
}

sub set_monitor_disabled {
    my ($config_dir, $id) = @_;
    my $s = read_monitor_state($config_dir, $id);
    $s->{status} = 'disabled';
    write_monitor_state($config_dir, $id, $s);
}

sub monitor_is_active {
    my ($config_dir, $id) = @_;
    my $s = read_monitor_state($config_dir, $id);
    return $s->{status} !~ /^(?:paused|disabled)$/;
}

1;
```

- [ ] **Step 4: Test ausführen (muss grün sein)**

```bash
perl t/test_monitor_state.pl 2>&1
```
Erwartet: `ok` für alle 8 Tests.

- [ ] **Step 5: Commit**

```bash
git add src/lib/monitor.pl t/test_monitor_state.pl
git commit -m "feat(monitor): monitor.pl state management + tests"
```

---

## Task 3: src/lib/query.pl + t/test_a2s_query.pl

**Files:**
- Create: `src/lib/query.pl`
- Create: `t/test_a2s_query.pl`

- [ ] **Step 1: Test-Datei schreiben**

`t/test_a2s_query.pl`:
```perl
#!/usr/bin/perl
use strict;
use warnings;
use FindBin qw($Bin);
use lib "$Bin/..";
chdir "$Bin/.." or die "Cannot chdir: $!";

print "1..5\n";
sub pass { print "ok - $_[0]\n" }
sub fail { print "not ok - $_[0]\n" }

require './src/lib/query.pl';

# Helper: build a minimal valid A2S_INFO response
# Header FF FF FF FF 49, protocol 0x11, then 4 null-terminated strings, 2-byte appid, players, max
sub _fake_a2s_response {
    my ($players, $max) = @_;
    my $hdr = "\xFF\xFF\xFF\xFF\x49\x11";
    my $strings = "My Server\x00mymap\x00myfolder\x00mygame\x00";
    my $appid   = pack('v', 730);  # CS2 app id, little-endian short
    my $counts  = pack('CCC', $players, $max, 0);  # players, max, bots
    return $hdr . $strings . $appid . $counts;
}

# 1. _parse_a2s_response: valid packet -> players and max
{
    my $raw = _fake_a2s_response(7, 20);
    my $r = _parse_a2s_response($raw);
    (defined $r && $r->{players} == 7 && $r->{max} == 20)
        ? pass('_parse_a2s_response: valid packet returns correct players/max')
        : fail("_parse_a2s_response: got " . (defined $r ? "p=$r->{players} m=$r->{max}" : 'undef'));
}

# 2. _parse_a2s_response: 0 players valid
{
    my $raw = _fake_a2s_response(0, 16);
    my $r = _parse_a2s_response($raw);
    (defined $r && $r->{players} == 0 && $r->{max} == 16)
        ? pass('_parse_a2s_response: 0 players is valid')
        : fail('_parse_a2s_response: 0 players failed');
}

# 3. _parse_a2s_response: too short -> undef
{
    my $r = _parse_a2s_response("\xFF\xFF\xFF\xFF\x49");
    !defined($r)
        ? pass('_parse_a2s_response: too short returns undef')
        : fail('_parse_a2s_response: too short should return undef');
}

# 4. _parse_a2s_response: wrong magic -> undef
{
    my $raw = _fake_a2s_response(3, 10);
    substr($raw, 4, 1) = "\x41";  # wrong type byte
    my $r = _parse_a2s_response($raw);
    !defined($r)
        ? pass('_parse_a2s_response: wrong type byte returns undef')
        : fail('_parse_a2s_response: wrong type byte should return undef');
}

# 5. _parse_a2s_response: truncated strings -> undef
{
    my $raw = "\xFF\xFF\xFF\xFF\x49\x11" . "NoNullTerminator";
    my $r = _parse_a2s_response($raw);
    !defined($r)
        ? pass('_parse_a2s_response: missing null terminator returns undef')
        : fail('_parse_a2s_response: missing null terminator should return undef');
}
```

- [ ] **Step 2: Test ausführen (muss scheitern)**

```bash
perl t/test_a2s_query.pl 2>&1
```
Erwartet: Fehler wegen fehlendem `./src/lib/query.pl`.

- [ ] **Step 3: src/lib/query.pl implementieren**

```perl
# LinuxGSM-WebCore - A2S query (Steam server query protocol)
use strict;
use warnings;

return 1 if defined &a2s_query;

# Parse a raw A2S_INFO response (FF FF FF FF 49 ...).
# Returns hashref {players, max} or undef on error.
sub _parse_a2s_response {
    my ($raw) = @_;
    return undef unless defined $raw && length($raw) >= 12;
    return undef unless substr($raw, 0, 5) eq "\xFF\xFF\xFF\xFF\x49";
    my $pos = 6;  # skip 4-byte header + type + protocol
    for (1..4) {
        my $null = index($raw, "\x00", $pos);
        return undef if $null < 0;
        $pos = $null + 1;
    }
    return undef if $pos + 4 > length($raw);
    $pos += 2;  # skip 2-byte app_id
    return undef if $pos + 1 >= length($raw);
    my $players = ord(substr($raw, $pos,     1));
    my $max     = ord(substr($raw, $pos + 1, 1));
    return { players => $players, max => $max };
}

# Query a game server via A2S_INFO (UDP).
# Returns hashref {players, max} or undef on timeout/error.
sub a2s_query {
    my ($host, $port, $timeout) = @_;
    $timeout //= 2;
    $port = int($port);
    return undef unless $port > 0 && $port < 65536;

    require IO::Socket::INET;
    my $sock = IO::Socket::INET->new(
        Proto    => 'udp',
        PeerAddr => $host,
        PeerPort => $port,
    ) or return undef;

    my $req = "\xFF\xFF\xFF\xFF\x54Source Engine Query\x00";
    $sock->send($req) or do { $sock->close(); return undef; };

    my $rin = '';
    vec($rin, fileno($sock), 1) = 1;
    my $ready = select($rin, undef, undef, $timeout);
    unless ($ready) { $sock->close(); return undef; }

    my $raw = '';
    $sock->recv($raw, 4096);
    $sock->close();

    return _parse_a2s_response($raw);
}

1;
```

- [ ] **Step 4: Test ausführen (muss grün sein)**

```bash
perl t/test_a2s_query.pl 2>&1
```
Erwartet: `ok` für alle 5 Tests.

- [ ] **Step 5: Commit**

```bash
git add src/lib/query.pl t/test_a2s_query.pl
git commit -m "feat(monitor): query.pl A2S implementation + tests"
```

---

## Task 4: src/scripts/query_a2s.pl

**Files:**
- Create: `src/scripts/query_a2s.pl`

- [ ] **Step 1: Skript schreiben**

`src/scripts/query_a2s.pl`:
```perl
#!/usr/bin/perl
use strict;
use warnings;

my ($host, $port) = @ARGV;
die "Usage: query_a2s.pl <host> <port>\n" unless $host && $port;

# Locate lib relative to this script
use FindBin qw($Bin);
require "$Bin/../lib/query.pl";

my $result = a2s_query($host, int($port), 2);
unless ($result) {
    print STDERR "A2S query failed: $host:$port\n";
    exit 1;
}
printf '{"players":%d,"max":%d}' . "\n", $result->{players}, $result->{max};
exit 0;
```

- [ ] **Step 2: Ausführbar machen und testen**

```bash
chmod +x src/scripts/query_a2s.pl
perl -c src/scripts/query_a2s.pl
```
Erwartet: `src/scripts/query_a2s.pl syntax OK`

- [ ] **Step 3: Commit**

```bash
git add src/scripts/query_a2s.pl
git commit -m "feat(monitor): query_a2s.pl CLI wrapper"
```

---

## Task 5: src/scripts/monitor_instance.sh

**Files:**
- Create: `src/scripts/monitor_instance.sh`

- [ ] **Step 1: Skript schreiben**

`src/scripts/monitor_instance.sh`:
```bash
#!/bin/bash
# monitor_instance.sh — check and optionally restart one game server instance
# Args: <instance_id> <user> <source> <server_dir> <script_name> <config_dir> <module_root>
set -euo pipefail

INSTANCE_ID="${1:?missing instance_id}"
UNIX_USER="${2:?missing user}"
SOURCE="${3:?missing source}"     # lgsm or steamcmd
SERVER_DIR="${4:?missing server_dir}"
SCRIPT_NAME="${5:?missing script_name}"
CONFIG_DIR="${6:?missing config_dir}"
MODULE_ROOT="${7:?missing module_root}"

STATE_DIR="$CONFIG_DIR/monitor/$INSTANCE_ID"
STATE_FILE="$STATE_DIR/state"
MAX_RESTARTS=5
WINDOW_SECS=3600
QUERY_TIMEOUT=2

# --- helpers ---------------------------------------------------------------

_read_state_key() {
    local key="$1" default="$2"
    if [ -f "$STATE_FILE" ]; then
        local v
        v=$(grep "^${key}=" "$STATE_FILE" 2>/dev/null | cut -d= -f2- | head -1)
        [ -n "$v" ] && echo "$v" && return
    fi
    echo "$default"
}

_write_state() {
    local status="$1" count="$2" window="$3"
    mkdir -p "$STATE_DIR"
    printf 'status=%s\nrestart_count=%s\nwindow_start=%s\n' \
        "$status" "$count" "$window" > "$STATE_FILE"
}

_log() { echo "[$(date '+%Y-%m-%d %T')] [$INSTANCE_ID] $*"; }

# --- state gate ------------------------------------------------------------

STATUS=$(_read_state_key status running)
if [[ "$STATUS" == "paused" || "$STATUS" == "disabled" ]]; then
    _log "Skipping — status=$STATUS"
    exit 0
fi

# --- LGSM path -------------------------------------------------------------

if [[ "$SOURCE" == "lgsm" ]]; then
    _log "LGSM: running ./$SCRIPT_NAME monitor"
    su -s /bin/bash -c "cd '$SERVER_DIR' && ./$SCRIPT_NAME monitor" "$UNIX_USER" 2>&1 || true
    # Read LGSM status after monitor
    lgsm_status=$(su -s /bin/bash -c "cd '$SERVER_DIR' && ./$SCRIPT_NAME status 2>/dev/null | grep -c 'Running'" "$UNIX_USER" 2>/dev/null || echo "0")
    if [[ "$lgsm_status" -gt 0 ]]; then
        _write_state "running" "0" "$(date +%s)"
        _log "LGSM: server running"
    else
        _log "LGSM: server offline after monitor"
        _write_state "failed" "99" "$(date +%s)"
    fi
    exit 0
fi

# --- Non-LGSM path ---------------------------------------------------------

# Source ports helper to get query port from instance config
INSTANCE_QUERY_PORT=0
if [ -f "$MODULE_ROOT/scripts/lib/ports.sh" ]; then
    export SERVER_DIR
    # shellcheck source=lib/ports.sh
    . "$MODULE_ROOT/scripts/lib/ports.sh"
    _resolve_instance_ports "$SCRIPT_NAME" 2>/dev/null || true
fi

# 1. PID check
PIDFILE="$SERVER_DIR/run.pid"
server_pid=0
if [ -f "$PIDFILE" ]; then
    server_pid=$(cat "$PIDFILE" 2>/dev/null | tr -d '[:space:]') || server_pid=0
fi

pid_alive=0
if [[ "$server_pid" -gt 0 ]] && kill -0 "$server_pid" 2>/dev/null; then
    pid_alive=1
fi

# 2. A2S query (only if PID alive and query port known)
freeze=0
if [[ "$pid_alive" -eq 1 && "${INSTANCE_QUERY_PORT:-0}" -gt 0 ]]; then
    if ! perl "$MODULE_ROOT/scripts/query_a2s.pl" "127.0.0.1" "$INSTANCE_QUERY_PORT" > /dev/null 2>&1; then
        _log "A2S timeout on port $INSTANCE_QUERY_PORT — freeze detected"
        freeze=1
    fi
fi

# 3. Server healthy?
if [[ "$pid_alive" -eq 1 && "$freeze" -eq 0 ]]; then
    _write_state "running" "0" "$(date +%s)"
    _log "OK"
    exit 0
fi

# 4. Restart flow
NOW=$(date +%s)
RESTART_COUNT=$(_read_state_key restart_count 0)
WINDOW_START=$(_read_state_key window_start "$NOW")

if [[ $((NOW - WINDOW_START)) -gt $WINDOW_SECS ]]; then
    _log "Window expired — resetting restart counter"
    RESTART_COUNT=0
    WINDOW_START=$NOW
fi

if [[ "$RESTART_COUNT" -ge "$MAX_RESTARTS" ]]; then
    _log "FAILED — $MAX_RESTARTS restarts in ${WINDOW_SECS}s"
    _write_state "failed" "$RESTART_COUNT" "$WINDOW_START"
    logger -t linuxgsm-webcore "Monitor: $INSTANCE_ID failed — restart limit reached"
    exit 1
fi

RESTART_COUNT=$((RESTART_COUNT + 1))
_log "Restart attempt $RESTART_COUNT/$MAX_RESTARTS"
_write_state "restarting" "$RESTART_COUNT" "$WINDOW_START"

# Kill stale PID and wineserver before restart
if [[ "$server_pid" -gt 0 ]]; then
    kill -TERM "$server_pid" 2>/dev/null || true
    sleep 2
    kill -KILL "$server_pid" 2>/dev/null || true
fi
WINEPREFIX="$SERVER_DIR/.wine"
if [ -d "$WINEPREFIX" ]; then
    WINEPREFIX="$WINEPREFIX" wineserver -k 2>/dev/null || true
fi

# Restart via steamcmd_control.sh
JOB_TMP=$(mktemp -d)
chmod 755 "$JOB_TMP"
MODULE_ROOT="$MODULE_ROOT" bash "$MODULE_ROOT/scripts/steamcmd_control.sh" \
    start "$JOB_TMP" "$UNIX_USER" "$SERVER_DIR" >/dev/null 2>&1 &
sleep 30

# Check if alive after restart
new_pid=0
[ -f "$PIDFILE" ] && new_pid=$(cat "$PIDFILE" 2>/dev/null | tr -d '[:space:]') || true
if [[ "$new_pid" -gt 0 ]] && kill -0 "$new_pid" 2>/dev/null; then
    _log "Restart successful"
    _write_state "running" "$RESTART_COUNT" "$WINDOW_START"
else
    _log "Restart failed — will retry at next cron tick"
    _write_state "restarting" "$RESTART_COUNT" "$WINDOW_START"
fi

rm -rf "$JOB_TMP"
exit 0
```

- [ ] **Step 2: Ausführbar + Syntax-Check**

```bash
chmod +x src/scripts/monitor_instance.sh
bash -n src/scripts/monitor_instance.sh
```
Erwartet: kein Fehler.

- [ ] **Step 3: Commit**

```bash
git add src/scripts/monitor_instance.sh
git commit -m "feat(monitor): monitor_instance.sh — LGSM delegation + Non-LGSM restart flow"
```

---

## Task 6: src/scripts/monitor_all.sh

**Files:**
- Create: `src/scripts/monitor_all.sh`

- [ ] **Step 1: Skript schreiben**

`src/scripts/monitor_all.sh`:
```bash
#!/bin/bash
# monitor_all.sh — watchdog for all registered LinuxGSM-WebCore instances
# Runs every 2 minutes via /etc/cron.d/linuxgsm-webcore-monitor
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
MODULE_ROOT="$(dirname "$SCRIPT_DIR")"

WEBMIN_CONF_BASE="${WEBMIN_CONF_BASE:-/etc/webmin}"
MODULE_NAME="linuxgsm-webcore"
CONFIG_DIR="$WEBMIN_CONF_BASE/$MODULE_NAME"
INSTANCES_FILE="$CONFIG_DIR/instances"

if [ ! -f "$INSTANCES_FILE" ]; then
    echo "[$(date '+%Y-%m-%d %T')] No instances file at $INSTANCES_FILE — nothing to do"
    exit 0
fi

while IFS=$'\t' read -r inst_id unix_user script_path source sftp_user rest; do
    # Skip comment lines and blank lines
    [[ "$inst_id" =~ ^#     ]] && continue
    [[ -z "$inst_id"         ]] && continue
    # Skip legacy key=value format (no tab)
    [[ "$inst_id" == *"="*  ]] && continue

    source="${source:-lgsm}"
    # Derive server_dir from script_path
    server_dir="${script_path%/*}"
    script_name="${script_path##*/}"

    # Skip if server_dir doesn't exist
    [ -d "$server_dir" ] || continue

    echo "[$(date '+%Y-%m-%d %T')] Monitoring $inst_id (user=$unix_user source=$source)"
    bash "$SCRIPT_DIR/monitor_instance.sh" \
        "$inst_id" "$unix_user" "$source" "$server_dir" "$script_name" \
        "$CONFIG_DIR" "$MODULE_ROOT" 2>&1 &

done < "$INSTANCES_FILE"

# Wait for all background monitor_instance.sh calls
wait
echo "[$(date '+%Y-%m-%d %T')] Monitor run complete"
exit 0
```

- [ ] **Step 2: Ausführbar + Syntax-Check**

```bash
chmod +x src/scripts/monitor_all.sh
bash -n src/scripts/monitor_all.sh
```
Erwartet: kein Fehler.

- [ ] **Step 3: Cron-Datei-Template erstellen**

`src/scripts/linuxgsm-webcore-monitor.cron`:
```
# LinuxGSM-WebCore monitor — runs every 2 minutes as root
# Install: cp linuxgsm-webcore-monitor.cron /etc/cron.d/linuxgsm-webcore-monitor
*/2 * * * * root /usr/share/webmin/linuxgsm-webcore/scripts/monitor_all.sh >> /var/log/linuxgsm-webcore-monitor.log 2>&1
```

- [ ] **Step 4: Syntax-Check**

```bash
bash -n src/scripts/monitor_all.sh && echo "OK"
```
Erwartet: `OK`

- [ ] **Step 5: Commit**

```bash
git add src/scripts/monitor_all.sh src/scripts/linuxgsm-webcore-monitor.cron
git commit -m "feat(monitor): monitor_all.sh watchdog + cron template"
```

---

## Task 7: manage.cgi — Monitor-Integration

**Files:**
- Modify: `src/manage.cgi`

Dieser Task integriert drei Dinge:
1. `require './lib/monitor.pl'` und `require './lib/query.pl'` am Anfang
2. State-Writes bei start/stop Aktionen
3. Player-Count + Monitor-Status-Badge im Server-Info-Block
4. Monitor-Reset- und Disable-Buttons

- [ ] **Step 1: require-Zeilen ergänzen**

In `src/manage.cgi` nach der Zeile `require './lib/logging.pl';` (oder nach den letzten require-Zeilen im Header):

```perl
require './lib/monitor.pl';
require './lib/query.pl';
```

Auch `$config_directory` in der `our`-Deklaration prüfen — muss enthalten sein.

- [ ] **Step 2: State-Write bei start/stop**

Im Block `elsif ($action eq 'start' || $action eq 'stop')` (Zeile ~703):

Nach der steamcmd-Variante (`&system_logged(...)` für steamcmd start/stop), direkt vor dem `&redirect(...)`:
```perl
if ($action eq 'stop') {
    &set_monitor_paused($config_directory, $instance_id);
} else {
    &set_monitor_running($config_directory, $instance_id);
}
```

Das Gleiche für die LGSM-Variante (`&run_server_action(...)` Block), ebenfalls direkt vor dem `&redirect(...)`:
```perl
if ($action eq 'stop') {
    &set_monitor_paused($config_directory, $instance_id);
} else {
    &set_monitor_running($config_directory, $instance_id);
}
```

- [ ] **Step 3: Monitor-Reset und Disable Actions**

Im Action-Dispatcher (nach dem letzten `elsif ($action eq ...)` Block, vor dem `else`):

```perl
elsif ($action eq 'monitor_reset') {
    &set_monitor_running($config_directory, $instance_id);
    &redirect("manage.cgi?instance_id=" . &html_escape($instance_id) . "&xnavigation=1");
    exit;
}
elsif ($action eq 'monitor_disable') {
    &set_monitor_disabled($config_directory, $instance_id);
    &redirect("manage.cgi?instance_id=" . &html_escape($instance_id) . "&xnavigation=1");
    exit;
}
```

- [ ] **Step 4: Monitor-Status-Zeile + Player-Count in der Server-Info-Tabelle**

Nach der Zeile `print &ui_table_row($text{'manage_status'}, _runtime_status_badge_html($runtime_status));` einfügen:

```perl
# Monitor status row
my $mon_state = &read_monitor_state($config_directory, $instance_id);
my $mon_status_key = 'monitor_status_' . ($mon_state->{status} // 'running');
my $mon_label = $text{$mon_status_key} || $mon_state->{status};
print &ui_table_row($text{'monitor_col'}, &html_escape($mon_label));

# Player count — only when server is running and game supports A2S
if ($runtime_status eq 'online' || $runtime_status eq 'running') {
    require './lib/games_meta.pl';
    my $qfield = &get_game_query_port_field($script_name_for_cfg);
    if ($qfield) {
        my $qport = int($cfg{$qfield} // 0);
        if ($qport > 0) {
            my $qdata = &a2s_query('127.0.0.1', $qport, 2);
            if ($qdata) {
                print &ui_table_row(
                    $text{'monitor_players'},
                    &html_escape("$qdata->{players} / $qdata->{max}")
                );
            }
        }
    }
}
```

- [ ] **Step 5: Monitor-Buttons unterhalb der Tabelle**

Nach `&ui_table_end()` (nach dem Firewall-Block), Monitor-Buttons für Admins ausgeben.

Suche den Block wo die Firewall-Buttons gerendert werden und füge danach ein:

```perl
# Monitor reset / disable buttons (admin only)
if (&is_admin()) {
    my $mon_s = ($mon_state->{status} // 'running');
    if ($mon_s eq 'failed' || $mon_s eq 'paused') {
        print &ui_form_start('manage.cgi', 'post');
        print &ui_hidden('instance_id', $instance_id);
        print &ui_hidden('action', 'monitor_reset');
        print &ui_submit($text{'monitor_reset_btn'}, undef, undef, undef, 'btn-success');
        print &ui_form_end();
    }
    if ($mon_s ne 'disabled') {
        print &ui_form_start('manage.cgi', 'post');
        print &ui_hidden('instance_id', $instance_id);
        print &ui_hidden('action', 'monitor_disable');
        print &ui_submit($text{'monitor_disable_btn'}, undef, undef, undef, 'btn-default');
        print &ui_form_end();
    }
    if ($mon_s eq 'disabled') {
        print &ui_form_start('manage.cgi', 'post');
        print &ui_hidden('instance_id', $instance_id);
        print &ui_hidden('action', 'monitor_reset');
        print &ui_submit($text{'monitor_reset_btn'}, undef, undef, undef, 'btn-success');
        print &ui_form_end();
    }
}
```

- [ ] **Step 6: Syntax-Check**

```bash
perl -c src/manage.cgi
```
Erwartet: `src/manage.cgi syntax OK`

- [ ] **Step 7: Commit**

```bash
git add src/manage.cgi
git commit -m "feat(monitor): manage.cgi — player count, monitor state, reset/disable buttons"
```

---

## Task 8: index.cgi — Monitor-Badge in Instanzliste

**Files:**
- Modify: `src/index.cgi`

- [ ] **Step 1: require monitor.pl ergänzen**

In `src/index.cgi` nach `require './lib/acl.pl';`:
```perl
require './lib/monitor.pl';
```

- [ ] **Step 2: Monitor-Badge-Funktion ergänzen**

In `src/index.cgi` nach `_instance_status_badge` Funktion:

```perl
sub _monitor_status_badge {
    my ($inst, $config_directory) = @_;
    my $id = $inst->{'id'};
    my $s = &read_monitor_state($config_directory, $id);
    my %map = (
        running   => '&#x1F7E2;',
        restarting => '&#x1F7E1; ' . ($text{'jobs_status_running'} || '...'),
        failed    => '&#x1F534; ' . ($text{'monitor_status_failed'} || 'Fehlgeschlagen'),
        paused    => '&#x26AB; ' . ($text{'monitor_status_paused'}  || 'Pausiert'),
        disabled  => '&#x26AB; ' . ($text{'monitor_status_disabled'} || 'Deaktiviert'),
    );
    return $map{$s->{status}} || '&#x1F7E2;';
}
```

- [ ] **Step 3: Monitor-Spalte in ui_columns_table**

In `src/index.cgi` den `push @rows, [...]` Block erweitern:

```perl
push @rows, [
    &html_escape($inst->{'user'}),
    &html_escape($inst->{'game'}),
    $port_str,
    _instance_status_badge($inst),
    _monitor_status_badge($inst, $config_directory),
    (&user_is_readonly($id)
        ? "<a href='$manage_url'>$text{'index_btn_manage'}</a> <small>(" . ($text{'acl_role_viewer'} || 'Viewer') . ")</small>"
        : "<a href='$manage_url'>$text{'index_btn_manage'}</a>"),
];
```

Und `ui_columns_table` Header entsprechend ergänzen:
```perl
print &ui_columns_table(
    [
        $text{'index_col_user'},
        $text{'index_col_game'},
        $text{'index_col_port'},
        ($text{'manage_status'} || 'Status'),
        ($text{'monitor_col'}   || 'Monitor'),
        $text{'index_col_manage'},
    ],
    "100%",
    \@rows,
);
```

- [ ] **Step 4: Syntax-Check**

```bash
perl -c src/index.cgi
```
Erwartet: `src/index.cgi syntax OK`

- [ ] **Step 5: Commit**

```bash
git add src/index.cgi
git commit -m "feat(monitor): index.cgi — monitor status badge in instance list"
```

---

## Task 9: Verifikation + Build

**Files:**
- Keine neuen Dateien

- [ ] **Step 1: Alle neuen Test-Dateien ausführen**

```bash
perl t/test_monitor_state.pl
perl t/test_a2s_query.pl
```
Erwartet: alle Tests `ok`.

- [ ] **Step 2: Standard-Verifikation**

```bash
bash scripts/verify.sh
```
Erwartet: keine Fehler, alle Syntax-Checks grün.

- [ ] **Step 3: Alle bestehenden Tests ausführen**

```bash
for f in t/test_*.pl; do echo "=== $f ==="; perl "$f" 2>&1; done
```
Erwartet: alle Tests grün, keine Regressionen.

- [ ] **Step 4: Build**

```bash
bash scripts/build.sh
```
Erwartet: `dist/linuxgsm-webcore-*.wbm` wird erzeugt.

- [ ] **Step 5: WBM-Inhalt prüfen**

```bash
tar -tzf dist/linuxgsm-webcore-*.wbm | grep -E "monitor|query" | sort
```
Erwartet: Einträge für `monitor.pl`, `query.pl`, `monitor_all.sh`, `monitor_instance.sh`, `query_a2s.pl`.

- [ ] **Step 6: Abschluss-Commit**

```bash
git add dist/
git commit -m "build: Monitor-Feature v1 — watchdog, A2S query, player count, state management"
```
