# LinuxGSM-WebCore Scaffold Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vollständige Projektstruktur mit Skeleton-Dateien für das LinuxGSM-WebCore Webmin-Plugin anlegen.

**Architecture:** `src/` enthält den Plugin-Code (CGI-Dateien im Root, Perl-Logik in `lib/`, Bash in `scripts/`). `packaging/` enthält Debian- und RPM-Packaging. Alle Dateien sind Skeletons mit minimalem, syntaktisch korrektem Inhalt.

**Tech Stack:** Perl 5 (Webmin CGI), Bash, debhelper 13, RPM spec

---

## Dateiübersicht

| Datei | Verantwortung |
|-------|---------------|
| `src/module.info` | Webmin-Modul-Metadaten |
| `src/index.cgi` | Dashboard-CGI |
| `src/provision.cgi` | Provisioning-CGI |
| `src/manage.cgi` | Server-Management-CGI |
| `src/config.cgi` | Modul-Einstellungen-CGI |
| `src/lib/core.pl` | Shared Helpers, Text-Loading |
| `src/lib/instance.pl` | Instanz-Erkennung via /etc/passwd |
| `src/lib/provision.pl` | User-Anlage, Port-Check, LGSM-Install |
| `src/lib/firewall.pl` | ufw/iptables via Webmin-API |
| `src/lib/sftp.pl` | SFTP-User + SSH-Config |
| `src/scripts/install_lgsm.sh` | LGSM herunterladen & installieren |
| `src/scripts/server_control.sh` | start/stop/restart/monitor/update |
| `src/scripts/engine_switch.sh` | Server-Executable tauschen |
| `src/lang/en` | Englische Basis-Texte |
| `src/lang/de` | Deutsche Übersetzungen |
| `src/images/icon.png` | Modul-Icon (Placeholder) |
| `packaging/debian/control` | Paket-Metadaten |
| `packaging/debian/rules` | Build-Regeln |
| `packaging/debian/changelog` | Debian-Changelog |
| `packaging/debian/install` | Datei-Mapping src/ → /usr/share/webmin/ |
| `packaging/debian/postinst` | Post-Install-Hook |
| `packaging/rpm/linuxgsm-webcore.spec` | RPM-Spec |
| `.gitignore` | Ignored files |
| `README.md` | Projekt-Übersicht |

---

### Task 1: Verzeichnisstruktur + .gitignore

**Files:**
- Create: `src/lib/.keep`, `src/scripts/.keep`, `src/lang/.keep`, `src/images/.keep`
- Create: `packaging/debian/.keep`, `packaging/rpm/.keep`
- Create: `.gitignore`

- [ ] **Step 1: Verzeichnisse anlegen**

```bash
mkdir -p src/lib src/scripts src/lang src/images
mkdir -p packaging/debian packaging/rpm
touch src/lib/.keep src/scripts/.keep src/lang/.keep src/images/.keep
touch packaging/debian/.keep packaging/rpm/.keep
```

- [ ] **Step 2: .gitignore schreiben**

Inhalt von `.gitignore`:
```
# Build-Artefakte
*.deb
*.rpm
*.tar.gz
*.buildinfo
*.changes

# Debian-Build
packaging/debian/linuxgsm-webcore/
packaging/debian/debhelper-build-stamp
packaging/debian/*.substvars
packaging/debian/*.log

# RPM-Build
packaging/rpm/BUILD/
packaging/rpm/RPMS/
packaging/rpm/SRPMS/
packaging/rpm/SOURCES/
packaging/rpm/SPECS/
packaging/rpm/BUILDROOT/

# Editor
*.swp
*~
.DS_Store
```

- [ ] **Step 3: Überprüfen**

```bash
ls -la src/ packaging/
```
Erwartung: Alle Unterverzeichnisse vorhanden.

- [ ] **Step 4: Commit**

```bash
git add .gitignore src/ packaging/
git commit --author="Christian Möllmann <moellix@knoellix.net>" -m "chore: Verzeichnisstruktur anlegen"
```

---

### Task 2: module.info + Sprachdateien

**Files:**
- Create: `src/module.info`
- Create: `src/lang/en`
- Create: `src/lang/de`

- [ ] **Step 1: module.info schreiben**

Inhalt von `src/module.info`:
```
name=linuxgsm-webcore
desc=LinuxGSM Game Server Manager
desc_de=LinuxGSM Game-Server-Verwaltung
version=0.1.0
depends=acl useradmin
category=servers
os_support=linux
```

- [ ] **Step 2: lang/en schreiben**

Inhalt von `src/lang/en`:
```
index_title=LinuxGSM Game Server Manager
index_desc=Manage game servers using LinuxGSM
index_no_instances=No game server instances found.
provision_title=Provision New Server
provision_game=Game
provision_user=System User
provision_port=Port
provision_submit=Provision
manage_title=Manage Server
manage_start=Start
manage_stop=Stop
manage_restart=Restart
manage_update=Update
manage_logs=View Logs
config_title=Module Settings
err_root=This action cannot be run as root.
err_not_found=Instance not found.
err_port_in_use=Port is already in use.
```

- [ ] **Step 3: lang/de schreiben**

Inhalt von `src/lang/de`:
```
index_title=LinuxGSM Game-Server-Verwaltung
index_desc=Game-Server mit LinuxGSM verwalten
index_no_instances=Keine Game-Server-Instanzen gefunden.
provision_title=Neuen Server anlegen
provision_game=Spiel
provision_user=System-Benutzer
provision_port=Port
provision_submit=Anlegen
manage_title=Server verwalten
manage_start=Starten
manage_stop=Stoppen
manage_restart=Neustart
manage_update=Aktualisieren
manage_logs=Logs anzeigen
config_title=Modul-Einstellungen
err_root=Diese Aktion darf nicht als root ausgeführt werden.
err_not_found=Instanz nicht gefunden.
err_port_in_use=Port ist bereits belegt.
```

- [ ] **Step 4: Webmin-Modul-Erkennung prüfen**

```bash
grep -c "=" src/lang/en src/lang/de
```
Erwartung: Beide Dateien haben dieselbe Anzahl Schlüssel (15).

- [ ] **Step 5: Commit**

```bash
git add src/module.info src/lang/
git commit --author="Christian Möllmann <moellix@knoellix.net>" -m "feat: module.info und Sprachdateien (en/de)"
```

---

### Task 3: CGI-Skeleton-Dateien

**Files:**
- Create: `src/index.cgi`
- Create: `src/provision.cgi`
- Create: `src/manage.cgi`
- Create: `src/config.cgi`

- [ ] **Step 1: index.cgi schreiben**

Inhalt von `src/index.cgi`:
```perl
#!/usr/bin/perl
use strict;
use warnings;

require './lib/core.pl';
require './lib/instance.pl';

our (%text, %config, %in);
&ReadParse(\%in);
&header($text{'index_title'}, '');

print "<p>$text{'index_desc'}</p>\n";

my @instances = &list_instances();
if (@instances) {
    print "<table class='ui_table'>\n";
    print "<tr><th>User</th><th>Game</th><th>Port</th><th>Status</th><th>Actions</th></tr>\n";
    foreach my $inst (@instances) {
        print "<tr><td>$inst->{'user'}</td><td>$inst->{'game'}</td>";
        print "<td>$inst->{'port'}</td><td>$inst->{'status'}</td>";
        print "<td><a href='manage.cgi?user=$inst->{user}'>Manage</a></td></tr>\n";
    }
    print "</table>\n";
} else {
    print "<p>$text{'index_no_instances'}</p>\n";
}

&footer('', '');
```

- [ ] **Step 2: provision.cgi schreiben**

Inhalt von `src/provision.cgi`:
```perl
#!/usr/bin/perl
use strict;
use warnings;

require './lib/core.pl';
require './lib/provision.pl';

our (%text, %config, %in);
&ReadParse(\%in);
&error_if_root();
&header($text{'provision_title'}, '');

if ($in{'submit'}) {
    my $user  = &sanitize_input($in{'user'});
    my $game  = &sanitize_input($in{'game'});
    my $port  = int($in{'port'});
    my $err   = &validate_provision($user, $game, $port);
    if ($err) {
        &error($err);
    } else {
        &provision_server($user, $game, $port);
        &redirect('index.cgi');
    }
} else {
    print "<form method='post' action='provision.cgi'>\n";
    print "<table>\n";
    print "<tr><td>$text{'provision_game'}</td><td><input name='game' type='text'></td></tr>\n";
    print "<tr><td>$text{'provision_user'}</td><td><input name='user' type='text'></td></tr>\n";
    print "<tr><td>$text{'provision_port'}</td><td><input name='port' type='number' value='27015'></td></tr>\n";
    print "</table>\n";
    print "<input type='submit' name='submit' value=\"$text{'provision_submit'}\">\n";
    print "</form>\n";
}

&footer('index.cgi', $text{'index_title'});
```

- [ ] **Step 3: manage.cgi schreiben**

Inhalt von `src/manage.cgi`:
```perl
#!/usr/bin/perl
use strict;
use warnings;

require './lib/core.pl';
require './lib/instance.pl';

our (%text, %config, %in);
&ReadParse(\%in);
&error_if_root();

my $user = &sanitize_input($in{'user'});
my $inst = &get_instance($user) or &error($text{'err_not_found'});

if ($in{'action'}) {
    my $action = &sanitize_input($in{'action'});
    &run_server_action($user, $action);
    &redirect("manage.cgi?user=$user");
}

&header("$text{'manage_title'}: $user", '');

print "<p>Game: $inst->{'game'} | Port: $inst->{'port'} | Status: $inst->{'status'}</p>\n";
foreach my $action (qw(start stop restart update)) {
    print "<form method='post' action='manage.cgi' style='display:inline'>\n";
    print "<input type='hidden' name='user' value='$user'>\n";
    print "<input type='hidden' name='action' value='$action'>\n";
    print "<input type='submit' value=\"$text{'manage_$action'}\">\n";
    print "</form>\n";
}

&footer('index.cgi', $text{'index_title'});
```

- [ ] **Step 4: config.cgi schreiben**

Inhalt von `src/config.cgi`:
```perl
#!/usr/bin/perl
use strict;
use warnings;

require './lib/core.pl';

our (%text, %config, %in);
&ReadParse(\%in);
&header($text{'config_title'}, '');

print "<p>$text{'config_title'}</p>\n";
# TODO: module configuration options

&footer('index.cgi', $text{'index_title'});
```

- [ ] **Step 5: Perl-Syntax prüfen**

```bash
perl -c src/index.cgi 2>&1
perl -c src/provision.cgi 2>&1
perl -c src/manage.cgi 2>&1
perl -c src/config.cgi 2>&1
```
Erwartung: Alle melden `syntax OK` (Webmin-Funktionen sind extern, Warnungen über unbekannte Symbole sind OK).

- [ ] **Step 6: Ausführbar machen**

```bash
chmod +x src/index.cgi src/provision.cgi src/manage.cgi src/config.cgi
```

- [ ] **Step 7: Commit**

```bash
git add src/*.cgi
git commit --author="Christian Möllmann <moellix@knoellix.net>" -m "feat: CGI-Skeleton-Dateien (index, provision, manage, config)"
```

---

### Task 4: lib/-Skeleton-Dateien

**Files:**
- Create: `src/lib/core.pl`
- Create: `src/lib/instance.pl`
- Create: `src/lib/provision.pl`
- Create: `src/lib/firewall.pl`
- Create: `src/lib/sftp.pl`

- [ ] **Step 1: core.pl schreiben**

Inhalt von `src/lib/core.pl`:
```perl
# LinuxGSM-WebCore - Shared helpers and Webmin API wrappers
use strict;
use warnings;

our (%text, %config, %gconfig);

# Load English base texts, then override with current language
&read_file("$module_root/lang/en", \%text);
if ($current_lang && $current_lang ne 'en') {
    &read_file("$module_root/lang/$current_lang", \%text);
}

# Load module config
&read_file("$config_directory/config", \%config) if -f "$config_directory/config";

# Prevent root execution of privileged actions
sub error_if_root {
    if ($< == 0 && !$config{'allow_root'}) {
        &error($text{'err_root'});
    }
}

# Strip dangerous characters from user input
sub sanitize_input {
    my ($input) = @_;
    $input =~ s/[^a-zA-Z0-9_\-]//g;
    return $input;
}

# Run a server action as the game user (never as root)
sub run_server_action {
    my ($user, $action) = @_;
    $user   = &sanitize_input($user);
    $action = &sanitize_input($action);
    return &system_logged("su -s /bin/bash -c \"./$user start\" $user") if $action eq 'start';
    return &system_logged("su -s /bin/bash -c \"./$user $action\" $user");
}

1;
```

- [ ] **Step 2: instance.pl schreiben**

Inhalt von `src/lib/instance.pl`:
```perl
# LinuxGSM-WebCore - Instance detection via /etc/passwd
use strict;
use warnings;

# Return list of all LGSM game server instances.
# An instance is a system user whose home dir contains a LinuxGSM script.
sub list_instances {
    my @instances;
    open(my $fh, '<', '/etc/passwd') or return ();
    while (<$fh>) {
        chomp;
        my ($user, undef, undef, undef, undef, $home, $shell) = split(':', $_);
        next unless $shell eq '/usr/sbin/nologin';
        next unless -f "$home/$user";  # LGSM script named after user
        my $inst = &get_instance($user);
        push @instances, $inst if $inst;
    }
    close($fh);
    return @instances;
}

# Return instance details for a specific user, or undef if not found.
sub get_instance {
    my ($user) = @_;
    $user = &sanitize_input($user);
    my @pw = getpwnam($user) or return undef;
    my $home = $pw[7];
    return undef unless -f "$home/$user";
    return {
        user   => $user,
        home   => $home,
        game   => _detect_game($home, $user),
        port   => _detect_port($home, $user),
        status => _detect_status($home, $user),
    };
}

sub _detect_game {
    my ($home, $user) = @_;
    # LGSM stores game ID in serverfiles/common.cfg or similar
    return 'unknown';  # TODO: parse LGSM config
}

sub _detect_port {
    my ($home, $user) = @_;
    return 0;  # TODO: parse LGSM config
}

sub _detect_status {
    my ($home, $user) = @_;
    my $rc = system("su -s /bin/bash -c \"./$user details\" $user 2>/dev/null | grep -q 'Online'");
    return $rc == 0 ? 'online' : 'offline';
}

1;
```

- [ ] **Step 3: provision.pl schreiben**

Inhalt von `src/lib/provision.pl`:
```perl
# LinuxGSM-WebCore - User provisioning, port checks, LGSM installation
use strict;
use warnings;

# Validate provisioning parameters. Returns error string or undef on success.
sub validate_provision {
    my ($user, $game, $port) = @_;
    return $text{'err_not_found'} unless $user && $game;
    return $text{'err_port_in_use'} if &port_in_use($port);
    return undef;
}

# Check if a port is already in use system-wide.
sub port_in_use {
    my ($port) = @_;
    $port = int($port);
    return 1 if system("ss -tuln | grep -q ':$port '") == 0;
    return 0;
}

# Provision a new game server instance.
# Creates system user, installs LGSM, runs install script.
# Never runs as root — uses su.
sub provision_server {
    my ($user, $game, $port) = @_;
    $user = &sanitize_input($user);
    $game = &sanitize_input($game);
    $port = int($port);

    # Create system user with nologin shell
    &system_logged("useradd -m -s /usr/sbin/nologin $user");

    # Install LGSM as game user
    my $script_path = "$module_root/../scripts/install_lgsm.sh";
    &system_logged("su -s /bin/bash -c 'bash $script_path $game' $user");

    return 1;
}

1;
```

- [ ] **Step 4: firewall.pl schreiben**

Inhalt von `src/lib/firewall.pl`:
```perl
# LinuxGSM-WebCore - Firewall management via ufw or iptables
use strict;
use warnings;

# Open a UDP+TCP port for a game server.
sub firewall_open_port {
    my ($port, $proto) = @_;
    $port  = int($port);
    $proto = ($proto && $proto eq 'udp') ? 'udp' : 'tcp';

    if (&has_ufw()) {
        &system_logged("ufw allow $port/$proto");
    } else {
        &system_logged("iptables -A INPUT -p $proto --dport $port -j ACCEPT");
    }
}

# Close a port when a game server is deprovisioned.
sub firewall_close_port {
    my ($port, $proto) = @_;
    $port  = int($port);
    $proto = ($proto && $proto eq 'udp') ? 'udp' : 'tcp';

    if (&has_ufw()) {
        &system_logged("ufw delete allow $port/$proto");
    } else {
        &system_logged("iptables -D INPUT -p $proto --dport $port -j ACCEPT");
    }
}

sub has_ufw {
    return -x '/usr/sbin/ufw';
}

1;
```

- [ ] **Step 5: sftp.pl schreiben**

Inhalt von `src/lib/sftp.pl`:
```perl
# LinuxGSM-WebCore - SFTP-only user management with SSH chroot
use strict;
use warnings;

my $SSHD_CONFIG = '/etc/ssh/sshd_config';
my $SFTP_MARKER = '# LinuxGSM-WebCore SFTP BEGIN';
my $SFTP_END    = '# LinuxGSM-WebCore SFTP END';

# Configure SFTP-only chroot access for a game user.
# Adds a Match block to sshd_config if not already present.
sub setup_sftp_user {
    my ($user) = @_;
    $user = &sanitize_input($user);

    # Set a random SFTP password (separate from system account)
    my $sftp_pass = &generate_sftp_password();
    &system_logged("echo '$user:$sftp_pass' | chpasswd");

    # Add SSH chroot block if not already configured
    unless (&sftp_configured($user)) {
        &append_sftp_config($user);
        &system_logged("systemctl reload sshd");
    }

    return $sftp_pass;
}

sub sftp_configured {
    my ($user) = @_;
    open(my $fh, '<', $SSHD_CONFIG) or return 0;
    while (<$fh>) { return 1 if /Match User $user/ }
    close($fh);
    return 0;
}

sub append_sftp_config {
    my ($user) = @_;
    my @pw = getpwnam($user) or return;
    my $home = $pw[7];
    open(my $fh, '>>', $SSHD_CONFIG) or return;
    print $fh "\n$SFTP_MARKER\n";
    print $fh "Match User $user\n";
    print $fh "    ChrootDirectory $home\n";
    print $fh "    ForceCommand internal-sftp\n";
    print $fh "    AllowTcpForwarding no\n";
    print $fh "    X11Forwarding no\n";
    print $fh "$SFTP_END\n";
    close($fh);
}

sub generate_sftp_password {
    my @chars = ('A'..'Z', 'a'..'z', '0'..'9');
    return join('', map { $chars[rand @chars] } 1..16);
}

1;
```

- [ ] **Step 6: Perl-Syntax prüfen**

```bash
perl -c src/lib/core.pl 2>&1
perl -c src/lib/instance.pl 2>&1
perl -c src/lib/provision.pl 2>&1
perl -c src/lib/firewall.pl 2>&1
perl -c src/lib/sftp.pl 2>&1
```
Erwartung: Alle melden `syntax OK`.

- [ ] **Step 7: Commit**

```bash
git add src/lib/
git commit --author="Christian Möllmann <moellix@knoellix.net>" -m "feat: lib/-Skeleton-Dateien (core, instance, provision, firewall, sftp)"
```

---

### Task 5: scripts/-Skeleton-Dateien

**Files:**
- Create: `src/scripts/install_lgsm.sh`
- Create: `src/scripts/server_control.sh`
- Create: `src/scripts/engine_switch.sh`

- [ ] **Step 1: install_lgsm.sh schreiben**

Inhalt von `src/scripts/install_lgsm.sh`:
```bash
#!/usr/bin/env bash
# install_lgsm.sh - Download and install LinuxGSM for a game server user
# Usage: install_lgsm.sh <game_id>
# Must be run as the game user (not root).
set -euo pipefail

GAME_ID="${1:?Usage: install_lgsm.sh <game_id>}"

# Validate game ID (alphanumeric only)
if [[ ! "$GAME_ID" =~ ^[a-zA-Z0-9]+$ ]]; then
    echo "ERROR: Invalid game ID: $GAME_ID" >&2
    exit 1
fi

# Refuse to run as root
if [[ "$EUID" -eq 0 ]]; then
    echo "ERROR: Must not run as root" >&2
    exit 1
fi

HOME_DIR="$(eval echo ~"$USER")"
cd "$HOME_DIR"

# Download LGSM
curl -Lo linuxgsm.sh "https://linuxgsm.sh" && chmod +x linuxgsm.sh

# Create game server script
bash linuxgsm.sh "$GAME_ID"

# Run auto-install
"./${GAME_ID}" auto-install
```

- [ ] **Step 2: server_control.sh schreiben**

Inhalt von `src/scripts/server_control.sh`:
```bash
#!/usr/bin/env bash
# server_control.sh - Control a LinuxGSM game server instance
# Usage: server_control.sh <user> <action>
# Actions: start stop restart monitor update
# Must be run as root (will su to game user).
set -euo pipefail

USER="${1:?Usage: server_control.sh <user> <action>}"
ACTION="${2:?Usage: server_control.sh <user> <action>}"

# Validate inputs (alphanumeric only)
if [[ ! "$USER" =~ ^[a-zA-Z0-9_-]+$ ]]; then
    echo "ERROR: Invalid user: $USER" >&2
    exit 1
fi

VALID_ACTIONS="start stop restart monitor update details"
if [[ ! " $VALID_ACTIONS " =~ " $ACTION " ]]; then
    echo "ERROR: Invalid action: $ACTION" >&2
    exit 1
fi

# Execute as game user — never as root
su -s /bin/bash -c "./$USER $ACTION" "$USER"
```

- [ ] **Step 3: engine_switch.sh schreiben**

Inhalt von `src/scripts/engine_switch.sh`:
```bash
#!/usr/bin/env bash
# engine_switch.sh - Switch server executable (e.g. Vanilla → Paper)
# Usage: engine_switch.sh <user> <new_engine>
# Must be run as root (will su to game user).
set -euo pipefail

USER="${1:?Usage: engine_switch.sh <user> <new_engine>}"
ENGINE="${2:?Usage: engine_switch.sh <user> <new_engine>}"

# Validate inputs
if [[ ! "$USER" =~ ^[a-zA-Z0-9_-]+$ ]]; then
    echo "ERROR: Invalid user: $USER" >&2
    exit 1
fi

if [[ ! "$ENGINE" =~ ^[a-zA-Z0-9_-]+$ ]]; then
    echo "ERROR: Invalid engine: $ENGINE" >&2
    exit 1
fi

HOME_DIR="$(getent passwd "$USER" | cut -d: -f6)"

# Stop server first
su -s /bin/bash -c "./$USER stop" "$USER" || true

# TODO: Download and replace engine binary
echo "INFO: Engine switch to $ENGINE not yet implemented" >&2

# Restart with new engine
su -s /bin/bash -c "./$USER start" "$USER"
```

- [ ] **Step 4: Bash-Syntax prüfen**

```bash
bash -n src/scripts/install_lgsm.sh 2>&1
bash -n src/scripts/server_control.sh 2>&1
bash -n src/scripts/engine_switch.sh 2>&1
```
Erwartung: Keine Ausgabe (= kein Syntaxfehler).

- [ ] **Step 5: Ausführbar machen**

```bash
chmod +x src/scripts/install_lgsm.sh src/scripts/server_control.sh src/scripts/engine_switch.sh
```

- [ ] **Step 6: Commit**

```bash
git add src/scripts/
git commit --author="Christian Möllmann <moellix@knoellix.net>" -m "feat: Bash-Scripts (install_lgsm, server_control, engine_switch)"
```

---

### Task 6: Debian-Packaging

**Files:**
- Create: `packaging/debian/control`
- Create: `packaging/debian/rules`
- Create: `packaging/debian/changelog`
- Create: `packaging/debian/install`
- Create: `packaging/debian/postinst`

- [ ] **Step 1: debian/control schreiben**

Inhalt von `packaging/debian/control`:
```
Source: linuxgsm-webcore
Section: admin
Priority: optional
Maintainer: Christian Möllmann <moellix@knoellix.net>
Build-Depends: debhelper-compat (= 13)
Standards-Version: 4.6.2
Homepage: https://github.com/knoellix/LinuxGSM-WebCore
Rules-Requires-Root: no

Package: linuxgsm-webcore
Architecture: all
Depends: webmin, perl, bash, curl, ${misc:Depends}
Description: LinuxGSM Game Server Manager for Webmin
 A Webmin plugin for managing game servers using LinuxGSM.
 Provides a web-based interface for provisioning, monitoring,
 and managing game server instances with strict user isolation
 and security (no-root, no-shell, SFTP-only access).
```

- [ ] **Step 2: debian/rules schreiben**

Inhalt von `packaging/debian/rules`:
```makefile
#!/usr/bin/make -f
%:
	dh $@
```

- [ ] **Step 3: debian/changelog schreiben**

Inhalt von `packaging/debian/changelog`:
```
linuxgsm-webcore (0.1.0-1) unstable; urgency=low

  * Initial release.

 -- Christian Möllmann <moellix@knoellix.net>  Tue, 08 Apr 2026 00:00:00 +0200
```

- [ ] **Step 4: debian/install schreiben**

Inhalt von `packaging/debian/install`:
```
src/* usr/share/webmin/linuxgsm-webcore/
```

- [ ] **Step 5: debian/postinst schreiben**

Inhalt von `packaging/debian/postinst`:
```bash
#!/bin/bash
set -e

if [ "$1" = "configure" ]; then
    # Reload Webmin if running to register the new module
    if [ -f /var/webmin/miniserv.pid ] && kill -0 "$(cat /var/webmin/miniserv.pid)" 2>/dev/null; then
        /etc/webmin/restart 2>/dev/null || true
    fi
fi

#DEBHELPER#
```

- [ ] **Step 6: Ausführbar machen**

```bash
chmod +x packaging/debian/rules packaging/debian/postinst
```

- [ ] **Step 7: Commit**

```bash
git add packaging/debian/
git commit --author="Christian Möllmann <moellix@knoellix.net>" -m "feat: Debian-Packaging-Dateien"
```

---

### Task 7: RPM-Spec

**Files:**
- Create: `packaging/rpm/linuxgsm-webcore.spec`

- [ ] **Step 1: RPM-Spec schreiben**

Inhalt von `packaging/rpm/linuxgsm-webcore.spec`:
```
Name:           linuxgsm-webcore
Version:        0.1.0
Release:        1%{?dist}
Summary:        LinuxGSM Game Server Manager for Webmin

License:        MIT
URL:            https://github.com/knoellix/LinuxGSM-WebCore
Source0:        %{name}-%{version}.tar.gz

BuildArch:      noarch
Requires:       webmin
Requires:       perl
Requires:       bash
Requires:       curl

%description
A Webmin plugin for managing game servers using LinuxGSM.
Provides a web-based interface for provisioning, monitoring,
and managing game server instances with strict user isolation
and security (no-root, no-shell, SFTP-only access).

%prep
%autosetup

%install
mkdir -p %{buildroot}/usr/share/webmin/linuxgsm-webcore
cp -r src/* %{buildroot}/usr/share/webmin/linuxgsm-webcore/

%post
if [ -f /var/webmin/miniserv.pid ] && kill -0 "$(cat /var/webmin/miniserv.pid)" 2>/dev/null; then
    /etc/webmin/restart 2>/dev/null || true
fi

%preun
# Remove SFTP config blocks added by the module on uninstall
# (handled by the module's own cleanup routine)
:

%files
%dir /usr/share/webmin/linuxgsm-webcore/
/usr/share/webmin/linuxgsm-webcore/*

%changelog
* Tue Apr 08 2026 Christian Möllmann <moellix@knoellix.net> - 0.1.0-1
- Initial package
```

- [ ] **Step 2: Spec auf offensichtliche Fehler prüfen**

```bash
grep -E "^(Name|Version|Release|Summary|License|Requires):" packaging/rpm/linuxgsm-webcore.spec
```
Erwartung: Alle Pflichtfelder sind vorhanden.

- [ ] **Step 3: Commit**

```bash
git add packaging/rpm/
git commit --author="Christian Möllmann <moellix@knoellix.net>" -m "feat: RPM-Spec für AlmaLinux/Rocky/Fedora"
```

---

### Task 8: README.md + Icon-Placeholder + Aufräumen

**Files:**
- Create: `README.md`
- Create: `src/images/icon.png` (Placeholder)
- Delete: `src/lib/.keep`, `src/scripts/.keep`, `src/lang/.keep`, `src/images/.keep`
- Delete: `packaging/debian/.keep`, `packaging/rpm/.keep`

- [ ] **Step 1: README.md schreiben**

Inhalt von `README.md`:
```markdown
# LinuxGSM-WebCore

A Webmin plugin for managing game servers via [LinuxGSM](https://linuxgsm.com/).

## Features

- Provision game server instances (isolated system users, no login shell)
- Start/Stop/Restart/Update via web UI
- SFTP-only file access (chrooted, separate password)
- Automatic firewall port management (ufw / iptables)
- Engine switching (e.g. Vanilla → Paper for Minecraft)

## Requirements

- Webmin
- Perl
- Bash
- curl

## Installation

### Debian/Ubuntu

```bash
dpkg -i linuxgsm-webcore_0.1.0-1_all.deb
```

### AlmaLinux/Rocky/Fedora

```bash
rpm -i linuxgsm-webcore-0.1.0-1.noarch.rpm
```

## Development

Source lives in `src/`, deployed to `/usr/share/webmin/linuxgsm-webcore/` by the package.

## Security

- Game users get `/usr/sbin/nologin` — no SSH login possible
- Plugin never runs game binaries as `root`
- SFTP access is chrooted to the user's home directory
- All inputs are sanitized before shell execution

## License

MIT
```

- [ ] **Step 2: Icon-Placeholder anlegen**

```bash
# Minimal 1x1 PNG als Placeholder (base64)
python3 -c "
import base64, pathlib
png_1x1 = base64.b64decode('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==')
pathlib.Path('src/images/icon.png').write_bytes(png_1x1)
"
```

- [ ] **Step 3: .keep-Dateien entfernen**

```bash
git rm src/lib/.keep src/scripts/.keep src/lang/.keep src/images/.keep
git rm packaging/debian/.keep packaging/rpm/.keep
```

- [ ] **Step 4: Gesamtstruktur prüfen**

```bash
find src/ packaging/ -not -name '.keep' | sort
```
Erwartung: Alle 22 Dateien aus der Übersicht oben sind vorhanden.

- [ ] **Step 5: Final Commit**

```bash
git add README.md src/images/icon.png
git commit --author="Christian Möllmann <moellix@knoellix.net>" -m "feat: README, Icon-Placeholder, .keep-Dateien entfernt — Scaffold komplett"
```

---

## Self-Review

**Spec-Abdeckung:**
- ✅ `src/module.info` — Task 2
- ✅ `src/index.cgi`, `provision.cgi`, `manage.cgi`, `config.cgi` — Task 3
- ✅ `src/lib/core.pl`, `instance.pl`, `provision.pl`, `firewall.pl`, `sftp.pl` — Task 4
- ✅ `src/scripts/install_lgsm.sh`, `server_control.sh`, `engine_switch.sh` — Task 5
- ✅ `src/lang/en` + `src/lang/de` — Task 2
- ✅ `src/images/icon.png` — Task 8
- ✅ `packaging/debian/` (5 Dateien) — Task 6
- ✅ `packaging/rpm/linuxgsm-webcore.spec` — Task 7
- ✅ `.gitignore`, `README.md` — Tasks 1 + 8

**Sicherheits-Invarianten (aus CLAUDE.md) geprüft:**
- ✅ `nologin`-Shell in `provision.pl`
- ✅ `error_if_root()` in `core.pl`, genutzt in `provision.cgi` + `manage.cgi`
- ✅ `su -s /bin/bash -c ...` in allen Shell-Aufrufen
- ✅ `sanitize_input()` in allen Perl-Dateien
- ✅ SFTP-Chroot in `sftp.pl`
