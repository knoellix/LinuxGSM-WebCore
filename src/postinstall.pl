#!/usr/bin/perl
# postinstall.pl — called by Webmin after module installation
# The monitor cron job is installed automatically when a server is first
# started via the UI (_ensure_monitor_cron in manage.cgi), not here.
1;
